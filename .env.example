# Database
# IMPORTANT: Make sure to include the database name "travel-planner" in the URI path
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/travel-planner?retryWrites=true&w=majority
MONGO_URI="mongodb+srv://username:password@cluster.mongodb.net/travel-planner?retryWrites=true&w=majority&appName=Cluster0"
DATABASE_URL="${MONGO_URI}"

# NextAuth
# For production (Vercel), this should be your actual domain: https://your-app.vercel.app
# For local development: http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"
# Generate a secure random string using: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key-change-this-in-production"

# OpenAI
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key-here"
